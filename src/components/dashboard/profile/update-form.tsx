"use client";
import React from "react";

type Props = {
  setIsOpen: React.Dispatch<React.SetStateAction<boolean>>;
};

export default function UpdateForm({ setIsOpen }: Props) {
  return <div>UpdateForm</div>;
}
